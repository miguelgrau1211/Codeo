{
  "$schema": "https://railway.app/backboard.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist/Front/browser -p $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
